name: Ping Render App

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Website
        run: |
          curl https://sitecheck-btjh.onrender.com/